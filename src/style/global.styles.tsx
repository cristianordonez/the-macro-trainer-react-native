import { StyleSheet } from 'react-native';

export const global = StyleSheet.create({
   screenContainer: {
      flex: 1,
      alignItems: 'center',
      justifyContent: 'center',
      padding: '5%',
   },
   textCenter: {
      textAlign: 'center',
   },
});

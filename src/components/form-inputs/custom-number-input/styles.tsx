import { Colors } from '@rneui/themed';
import { StyleSheet } from 'react-native';

export const makeNumberInputStyles = (colors: Colors) =>
   StyleSheet.create({
      container: {
         marginBottom: -5,
      },
   });

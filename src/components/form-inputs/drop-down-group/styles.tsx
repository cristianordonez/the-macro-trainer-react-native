import { StyleSheet } from 'react-native';

export const dropDownGroupStyles = StyleSheet.create({
   pickerContainer: {
      width: 125,
      fontSize: 30,
   },
   text: {
      fontSize: 16,
   },
});

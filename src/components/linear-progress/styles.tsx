import { StyleSheet } from 'react-native';

export const customProgressStyles = StyleSheet.create({
   container: {
      flexDirection: 'row',
      justifyContent: 'space-evenly',
   },
   progress: {
      width: '75%',
      alignSelf: 'center',
   },
});

import { StyleSheet } from 'react-native';

export const CustomSetCheckboxStyles = StyleSheet.create({
   container: {
      height: 35,
      width: 35,
      borderRadius: 50,
      alignItems: 'center',
      justifyContent: 'center',
   },
});
